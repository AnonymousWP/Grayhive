GRAYLOG_PASSWORD_SECRET=<your-password> # You can generate a password using `pwgen -N 1 -s 96`
GRAYLOG_ROOT_PASSWORD_SHA2=<hash-of-the-above-password> # You can generate the SHA256-hash of your password with `echo -n <password> | sha256sum`
GRAYLOG_HTTP_EXTERNAL_URI=http://<your-ip>/ # You can change the IP-address to 127.0.0.1 if you want to run it locally, or to your public IP-address if you want it to be accessible outside your local network. For example: `GRAYLOG_HTTP_EXTERNAL_URI=http://127.0.0.1/`
